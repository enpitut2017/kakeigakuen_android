<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorMain</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="LoginEditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="android:textColorHint">@color/placeholder</item>
        <item name="android:textColor">@color/placeholder</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <style name="LoginEditTextColorControle">
        <item name="android:colorControlNormal">@color/placeholder</item>
    </style>

    <style name="button_A" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/colorMain</item>
        <item name="android:background">@color/A_back</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <style name="MainEditText" parent="Widget.AppCompat.EditText">
        <item name="android:textColorHint">@color/mainplaceholder</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <style name="MainEditTextColorControle">
        <item name="android:colorControlNormal">@color/mainplaceholder</item>
    </style>

    <style name="ListBody">
        <item name="android:textColor">@color/listbody</item>
    </style>

    <style name="ListHeader">
        <item name="android:textColor">@color/listheader</item>
    </style>

    <style name="button_B" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/A_back</item>
        <item name="android:background">@color/colorMain</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

</resources>
